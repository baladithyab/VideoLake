| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |"""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |Bulk Video Processing Pipeline
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |Processes large video datasets in parallel with:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |- Configurable batch sizes
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |- Progress tracking and checkpointing
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |- Error recovery and retry logic
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |- Resource usage monitoring
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |- Cost tracking
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |Designed for stress testing vector stores with realistic workloads.
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |"""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |import time
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |import asyncio
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from concurrent.futures import ThreadPoolExecutor, as_completed
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from typing import Dict, List, Any, Optional, Callable
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from dataclasses import dataclass, field
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from datetime import datetime
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from pathlib import Path
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |import json
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from src.services.video_dataset_manager import VideoDatasetManager, VideoMetadata, VideoDatasetConfig
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from src.services.embedding_model_selector import EmbeddingModelSelector, EmbeddingModel, UnifiedEmbeddingResult
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from src.services.parallel_vector_store_comparison import ParallelVectorStoreComparison
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from src.utils.logging_config import get_logger
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |from src.exceptions import ProcessingError
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |logger = get_logger(__name__)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |@dataclass
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |class BulkProcessingConfig:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    """Configuration for bulk video processing."""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Dataset configuration
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    dataset_config: VideoDatasetConfig
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Embedding model selection
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    embedding_model: EmbeddingModel = EmbeddingModel.MARENGO
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    marengo_vector_types: List[str] = field(default_factory=lambda: ["visual-text"])
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    nova_dimension: int = 1024
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    nova_mode: str = "AUDIO_VIDEO_COMBINED"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Processing mode
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    embedding_only: bool = True  # If True, only generate embeddings (no vector store needed)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    save_embeddings_to_s3: bool = True  # Save embedding JSON to S3 for later use
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Vector store selection (only used if embedding_only=False)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    enabled_vector_stores: List[str] = field(default_factory=lambda: ["s3vector"])
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    lancedb_backend: str = "s3"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Processing configuration
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    max_concurrent_videos: int = 5
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    batch_size: int = 10
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    enable_checkpointing: bool = True
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    checkpoint_interval: int = 10
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Resource limits
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    max_processing_time_hours: int = 24
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    max_cost_usd: Optional[float] = None
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |@dataclass
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |class BulkProcessingMetrics:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    """Metrics for bulk video processing."""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    total_videos: int = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    processed_videos: int = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    failed_videos: int = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    skipped_videos: int = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Embedding metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    embeddings_generated: int = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    embedding_errors: int = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    total_embedding_dimensions: int = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Storage metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    vectors_stored: Dict[str, int] = field(default_factory=dict)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    storage_errors: Dict[str, int] = field(default_factory=dict)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Performance metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    total_processing_time_sec: float = 0.0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    avg_processing_time_sec: float = 0.0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    fastest_video_sec: Optional[float] = None
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    slowest_video_sec: Optional[float] = None
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Cost metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    estimated_total_cost_usd: float = 0.0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    cost_per_video_usd: float = 0.0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    # Timestamps
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    started_at: Optional[str] = None
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    last_update_at: Optional[str] = None
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    completed_at: Optional[str] = None
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    @property
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    def success_rate(self) -> float:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """Calculate success rate."""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        total = self.processed_videos + self.failed_videos
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        return (self.processed_videos / total * 100) if total > 0 else 0.0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |class BulkVideoProcessor:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    Bulk video processing pipeline for stress testing and large-scale demos.
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    Features:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    - Parallel video processing
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    - Configurable embedding models
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    - Multiple vector store storage
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    - Progress tracking and checkpointing
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    - Cost monitoring and limits
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    - Performance metrics collection
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    Example:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Process 100 videos from MSR-VTT with Marengo
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        config = BulkProcessingConfig(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            dataset_config=VideoDatasetManager.get_recommended_dataset("small_test"),
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            embedding_model=EmbeddingModel.MARENGO,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            marengo_vector_types=["visual-text", "audio"],
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            enabled_vector_stores=["s3vector", "qdrant"],
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            max_concurrent_videos=5
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        processor = BulkVideoProcessor(config)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        results = processor.process_dataset()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        print(f"Processed: {results.processed_videos}/{results.total_videos}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        print(f"Success rate: {results.success_rate}%")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        print(f"Total cost: ${results.estimated_total_cost_usd}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    def __init__(self, config: BulkProcessingConfig):
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Initialize bulk video processor.
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Args:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            config: Bulk processing configuration
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        self.config = config
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        self.logger = get_logger(__name__)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Initialize dataset manager
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        self.dataset_manager = VideoDatasetManager(config.dataset_config)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Initialize embedding model selector
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        if config.embedding_model == EmbeddingModel.MARENGO:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            self.embedding_selector = EmbeddingModelSelector(model=EmbeddingModel.MARENGO)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        else:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            self.embedding_selector = EmbeddingModelSelector(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                model=EmbeddingModel.NOVA,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                embedding_dimension=config.nova_dimension
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Initialize metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        self.metrics = BulkProcessingMetrics(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            started_at=datetime.utcnow().isoformat()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Initialize vector stores based on config
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        for store in config.enabled_vector_stores:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            self.metrics.vectors_stored[store] = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            self.metrics.storage_errors[store] = 0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        logger.info(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            f"Initialized bulk processor: dataset={config.dataset_config.name}, "
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            f"model={config.embedding_model.value}, "
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            f"stores={config.enabled_vector_stores}"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    def process_dataset(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        self,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        progress_callback: Optional[Callable[[BulkProcessingMetrics], None]] = None
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    ) -> BulkProcessingMetrics:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Process entire dataset with parallel execution.
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Args:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            progress_callback: Optional callback for progress updates
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Returns:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            BulkProcessingMetrics with complete results
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        logger.info(f"Starting bulk processing: {self.config.dataset_config.name}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        start_time = time.time()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        batch = []
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        try:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Stream videos from dataset
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            for video_metadata in self.dataset_manager.stream_and_upload():
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                batch.append(video_metadata)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                self.metrics.total_videos += 1
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                # Process batch when full
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                if len(batch) >= self.config.batch_size:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    self._process_batch(batch)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    batch = []
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    # Progress callback
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    if progress_callback:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        progress_callback(self.metrics)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    # Check resource limits
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    self._check_resource_limits()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Process remaining videos
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if batch:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                self._process_batch(batch)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Finalize metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            self.metrics.total_processing_time_sec = time.time() - start_time
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            self.metrics.completed_at = datetime.utcnow().isoformat()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if self.metrics.processed_videos > 0:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                self.metrics.avg_processing_time_sec = (
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    self.metrics.total_processing_time_sec / self.metrics.processed_videos
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                self.metrics.cost_per_video_usd = (
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    self.metrics.estimated_total_cost_usd / self.metrics.processed_videos
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            logger.info(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                f"Bulk processing complete: {self.metrics.processed_videos} videos, "
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                f"{self.metrics.success_rate}% success rate, "
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                f"${self.metrics.estimated_total_cost_usd:.2f} total cost"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            return self.metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        except Exception as e:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            logger.error(f"Bulk processing failed: {str(e)}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            raise ProcessingError(f"Bulk processing error: {str(e)}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    def _process_batch(self, batch: List[VideoMetadata]) -> None:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """Process a batch of videos in parallel."""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        logger.info(f"Processing batch of {len(batch)} videos")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        with ThreadPoolExecutor(max_workers=self.config.max_concurrent_videos) as executor:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            futures = {
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                executor.submit(self._process_single_video, video): video
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                for video in batch
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            }
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            for future in as_completed(futures):
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                video = futures[future]
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                try:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    result = future.result()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    if result['success']:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        self.metrics.processed_videos += 1
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        self.metrics.embeddings_generated += result.get('embeddings_count', 0)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        # Update storage metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        for store in result.get('stored_in', []):
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                            self.metrics.vectors_stored[store] += 1
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    else:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        self.metrics.failed_videos += 1
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    # Update cost
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    self.metrics.estimated_total_cost_usd += result.get('cost_usd', 0)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                except Exception as e:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    logger.error(f"Batch processing error for {video.video_id}: {str(e)}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    self.metrics.failed_videos += 1
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        self.metrics.last_update_at = datetime.utcnow().isoformat()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    def _process_single_video(self, video: VideoMetadata) -> Dict[str, Any]:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Process a single video: generate embeddings and store.
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Args:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            video: Video metadata with S3 URI
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        Returns:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            Processing result dictionary
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        if not video.s3_uri or not video.uploaded_to_s3:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            return {'success': False, 'reason': 'not_uploaded'}
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        start_time = time.time()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        result = {
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            'video_id': video.video_id,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            'success': False,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            'embeddings_count': 0,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            'stored_in': [],
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            'cost_usd': 0.0
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        }
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        try:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Generate embeddings
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if self.config.embedding_model == EmbeddingModel.MARENGO:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                embedding_result = self.embedding_selector.process_video(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    video_uri=video.s3_uri,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    vector_types=self.config.marengo_vector_types
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            else:  # Nova
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                embedding_result = self.embedding_selector.process_video(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    video_uri=video.s3_uri,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    embedding_mode=self.config.nova_mode
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            result['embeddings_count'] = embedding_result.total_embedding_count
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            result['success'] = True
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Estimate embedding cost
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Marengo: ~$0.35 per minute
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Nova: ~$0.008 per 1000 input tokens
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            video_duration_min = (video.duration_sec or 60) / 60
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if self.config.embedding_model == EmbeddingModel.MARENGO:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                result['cost_usd'] = video_duration_min * 0.35 * len(self.config.marengo_vector_types)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            else:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                result['cost_usd'] = video_duration_min * 0.02  # Estimated
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Save embeddings to S3 for later use (if enabled)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if self.config.save_embeddings_to_s3:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                embedding_s3_key = f"{self.config.dataset_config.s3_prefix}embeddings/{self.config.dataset_config.name}/{video.video_id}.json"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                try:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    import boto3
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    s3_client = boto3.client('s3')
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    embedding_data = {
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        'video_id': video.video_id,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        'video_s3_uri': video.s3_uri,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        'model': self.config.embedding_model.value,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        'embeddings': {
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                            key: emb.tolist() if hasattr(emb, 'tolist') else emb
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                            for key, emb in embedding_result.embeddings.items()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        },
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        'metadata': {
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                            'dimensions': embedding_result.dimensions,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                            'processing_time_ms': embedding_result.processing_time_ms,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                            'generated_at': datetime.utcnow().isoformat()
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        }
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    }
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    s3_client.put_object(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        Bucket=self.config.dataset_config.s3_bucket,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        Key=embedding_s3_key,
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        Body=json.dumps(embedding_data),
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        ContentType='application/json'
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    result['embedding_s3_uri'] = f"s3://{self.config.dataset_config.s3_bucket}/{embedding_s3_key}"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    logger.debug(f"Saved embeddings to {result['embedding_s3_uri']}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                except Exception as e:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    logger.warning(f"Failed to save embeddings to S3: {str(e)}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Store in vector stores (only if embedding_only=False)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if not self.config.embedding_only:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                for store in self.config.enabled_vector_stores:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    try:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        # TODO: Implement actual vector store insertion
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        # This would call the appropriate provider to store embeddings
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        # For now, just mark as stored
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        result['stored_in'].append(store)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        logger.debug(f"Would store in {store} (not implemented)")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    except Exception as e:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        logger.error(f"Storage failed in {store}: {str(e)}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                        self.metrics.storage_errors[store] += 1
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            else:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                # Embedding-only mode - no vector store setup needed
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                result['stored_in'] = []
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                logger.debug(f"Embedding-only mode: skipping vector store storage")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            processing_time = time.time() - start_time
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            # Update performance metrics
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if self.metrics.fastest_video_sec is None or processing_time < self.metrics.fastest_video_sec:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                self.metrics.fastest_video_sec = processing_time
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if self.metrics.slowest_video_sec is None or processing_time > self.metrics.slowest_video_sec:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                self.metrics.slowest_video_sec = processing_time
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            return result
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        except Exception as e:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            logger.error(f"Failed to process {video.video_id}: {str(e)}")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            result['success'] = False
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            result['error'] = str(e)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            return result
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    def _check_resource_limits(self) -> None:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """Check if resource limits have been exceeded."""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Check cost limit
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        if self.config.max_cost_usd:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if self.metrics.estimated_total_cost_usd >= self.config.max_cost_usd:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                raise ProcessingError(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    f"Cost limit exceeded: ${self.metrics.estimated_total_cost_usd:.2f} >= ${self.config.max_cost_usd}"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Check time limit
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        if self.metrics.started_at:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            elapsed_hours = (time.time() - time.mktime(time.strptime(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                self.metrics.started_at, "%Y-%m-%dT%H:%M:%S.%f"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            ))) / 3600
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            if elapsed_hours >= self.config.max_processing_time_hours:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                raise ProcessingError(
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                    f"Time limit exceeded: {elapsed_hours:.1f}h >= {self.config.max_processing_time_hours}h"
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |                )
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |    def generate_report(self) -> str:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        """Generate markdown report of bulk processing results."""
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report = []
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"# Bulk Video Processing Report\n\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"**Dataset**: {self.config.dataset_config.name}\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"**Embedding Model**: {self.config.embedding_model.value.upper()}\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"**Generated**: {datetime.utcnow().isoformat()}\n\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Summary
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append("## Summary\n\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Metric | Value |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"|--------|-------|\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Total Videos | {self.metrics.total_videos} |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Successfully Processed | {self.metrics.processed_videos} |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Failed | {self.metrics.failed_videos} |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Success Rate | {self.metrics.success_rate:.1f}% |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Embeddings Generated | {self.metrics.embeddings_generated} |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Total Cost | ${self.metrics.estimated_total_cost_usd:.2f} |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Cost per Video | ${self.metrics.cost_per_video_usd:.4f} |\n\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Performance
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append("## Performance\n\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Metric | Value |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"|--------|-------|\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Total Time | {self.metrics.total_processing_time_sec/3600:.2f} hours |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Avg Time/Video | {self.metrics.avg_processing_time_sec:.2f}s |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Fastest Video | {self.metrics.fastest_video_sec:.2f}s |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Slowest Video | {self.metrics.slowest_video_sec:.2f}s |\n\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        # Vector store results
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append("## Vector Store Results\n\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"| Store | Vectors Stored | Errors |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        report.append(f"|-------|---------------|--------|\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        for store in self.config.enabled_vector_stores:
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            stored = self.metrics.vectors_stored.get(store, 0)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            errors = self.metrics.storage_errors.get(store, 0)
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |            report.append(f"| {store.upper()} | {stored} | {errors} |\n")
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |
| Slowest Video | {self.metrics.slowest_video_sec:.2f if self.metrics.slowest_video_sec else 0:.2f}s || Fastest Video | {self.metrics.fastest_video_sec:.2f if self.metrics.fastest_video_sec else 0:.2f}s |        return ''.join(report)
